# Claude Code instructions

General agent rules and PR workflow live in @AGENTS.md. Claude Code
follows them the same as any other agent — read those first.

Use this file for Claude Code-specific instructions only: hooks, MCP configuration, slash
commands, or behaviour that applies only when running through Claude Code. Anything that
applies to all agents belongs in `AGENTS.md`, not here.
